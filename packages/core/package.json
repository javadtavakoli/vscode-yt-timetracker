{
  "name": "@ylate/core",
  "version": "0.0.0",
  "private": true,
  "description": "Ylate shared core — pure timer state machine + YouTrack HTTP client. Runs in Node, browsers, Electron, and Tauri.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -p ./ --watch"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
