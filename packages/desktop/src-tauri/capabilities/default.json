{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Ylate window: tray manipulation, store read/write, autostart toggle, and IPC for the renderer-side host.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "core:app:default",
    "core:tray:default",
    "store:default",
    "autostart:default"
  ]
}
