# Tauri / Cargo build outputs
src-tauri/target/
src-tauri/gen/
# Cargo.lock IS committed for binary crates (this one) — do not gitignore

# Mobile icon sets (we don't build Android/iOS yet — `pnpm icons` generates
# them but they're noise in this repo).
src-tauri/icons/android/
src-tauri/icons/ios/
src-tauri/icons/Square*.png
src-tauri/icons/StoreLogo.png

# Build output
dist/
