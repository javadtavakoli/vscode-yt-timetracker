{
  "name": "ylate-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Ylate — YouTrack time tracker. Monorepo root.",
  "scripts": {
    "build": "turbo run build",
    "compile": "turbo run build",
    "watch": "turbo run watch",
    "package": "turbo run package",
    "install:vscode": "turbo run install:vscode"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@11.1.3"
}